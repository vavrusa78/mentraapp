# Pin Node version and install build tools
[phases.setup]
nixPkgs = ["nodejs_20", "pnpm", "git"]

[phases.install]
cmds = ["npm ci || npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
